# 2026-06-25 - WooSync UX Overhaul

## Task: WooSync UX overhaul (1c001503-e92b-4910-ac88-80707a26ee28)

### What was done

Completed comprehensive UX overhaul of the WooSync WordPress plugin (amrod-sync):

**1. Onboarding Wizard (new)**
- Created `assets/js/wizard.js` with clean 3-step wizard flow
- Step 1: Welcome screen with logo, tagline, and "Get Started" button
- Step 2: Empty vendor state with "Add Your First Vendor" grid (no pre-filled vendors)
- Step 3: Credential form with contextual vendor support panel
- Wizard stored in localStorage to not repeat after completion
- Triggered on plugin activation with "Run Setup Wizard" link in notice

**2. Vendor Support Panel**
- Added `amrod_get_vendor_templates()` function with per-vendor support data
- Amrod: support@amrod.co.za, docs at newapidocs.amrod.co.za, auth/API URLs
- SMD: placeholder support (user to provide)
- Barron: placeholder support (user to provide)
- Support panel rendered in wizard Step 3 with email, docs, phone, address

**3. Documentation Links**
- Added "Help & Documentation" section in WooSync Settings page
- Links: Amrod API Docs, WooCommerce REST API, WooSync User Guide, Get Support
- Styled as buttons, open in new tab

**4. Colour Scheme Overhaul**
- Completely replaced `assets/css/admin.css` with new professional palette
- Primary: #4338CA (indigo), Primary Light: #6366F1
- Accent: #0EA5E9 (sky blue), Success: #10B981
- Warning: #F59E0B, Danger: #EF4444
- Background: #F8FAFC, Card BG: #FFFFFF
- Text Primary: #1E293B, Text Secondary: #64748B
- CSS variables for consistency, gradient card headers, new border-radius

**5. New WooSync Logo**
- Created `assets/images/woosync-logo.svg` with indigo color scheme
- W + S letters with sync arrows icon, indigo gradient

**6. GitHub Auto-Update Fix**
- Created `includes/class-woosync-updater.php` with WooSync_Updater class
- Points to correct repo: https://github.com/preneshnaidoo/woosync
- Reads GitHub releases API, extracts .zip from release assets
- Uses `amrod_auto_update` option to enable/disable
- Caches release info for 1 hour

**7. Clean Admin Menu**
- Simplified to 4 tabs: Dashboard, Connect & Map, Sync Log, Settings
- Removed separate menu items for Promotions, Promo Share, Pricing, Updates
- These remain accessible via their URL paths within Settings context

**8. Other Changes**
- Version bump to 3.4.0
- Added `amrod_save_credentials_simple` AJAX handler for wizard
- Wizard trigger on activation via admin_init hook
- Updated CHANGELOG.md with full v3.4.0 changelog

### Files Changed
- `amrod-sync.php` - main plugin file (version, menu, wizard, templates, updater include)
- `assets/css/admin.css` - complete new professional color palette
- `assets/js/wizard.js` - new onboarding wizard (21KB)
- `assets/images/woosync-logo.svg` - new indigo logo
- `includes/class-woosync-updater.php` - new GitHub auto-updater
- `CHANGELOG.md` - updated with v3.4.0 changelog

### GitHub Upload
- Packaged as `/tmp/woosync-v1.0.1.zip` (67KB)
- GitHub API rate limit hit - credentials not configured
- User needs to add GitHub in Settings > Integrations for upload to work
- After GitHub credentials added: create release v1.0.0, upload zip as asset

## Task: Dynamic Credential Fields Per Vendor (1f19542b-934f-4740-be6d-b1e8d9a9a1bf)

### What was done

Fixed the WooSync wizard credential form to show DIFFERENT fields based on selected vendor:

**Problem:** The wizard showed Amrod fields (Username, Password, Customer Code) for ALL vendors including SMD which uses Bearer Token + ClientAccessKey authentication.

**Solution implemented:**

1. **amrod-sync.php** (v3.4.1):
   - Added `amrod_get_vendor_credential_schemas()` function with field definitions for Amrod, SMD, Barron, Custom
   - Each schema: auth_type, label, description, fields array, support info, test_type
   - Added `amrod_get_credential_schema($vendor_id)` helper
   - Updated SMD vendor template with api_base_url and auth_type='bearer_key'
   - New `amrod_ajax_get_credential_schema` AJAX handler
   - Updated `amrod_ajax_test_connection` to handle vendor_login, bearer_key, custom auth types
   - Updated `amrod_ajax_save_credentials_simple` with vendor-specific validation
   - Added `woosync_vendor_credentials` option for per-vendor credential storage
   - Updated `wp_localize_script` to include vendorCredentialSchemas

2. **assets/js/wizard.js** (649 lines):
   - `renderCredentialsForm()` renders fields dynamically from schema
   - `renderSupportPanel()` shows vendor-specific support info with color-coded headers
   - Vendor auth badges in Step 1 vendor selection
   - All form fields sent to AJAX handlers dynamically
   - Password toggle buttons for all password fields
   - Vendor-specific support panel colors

3. **assets/css/admin.css** (+170 lines):
   - Dynamic credential field styles
   - Vendor support panel color variations (Amrod=indigo, SMD=purple, Barron=amber, Custom=gray)
   - Vendor auth badge styles
   - Password field styles
   - Vendor auth note styles
   - Responsive wizard styles

4. **CHANGELOG.md** — updated with v3.4.1 changelog

### Files Changed
- `amrod-sync.php` — added credential schemas, updated AJAX handlers
- `assets/js/wizard.js` — dynamic field rendering, vendor-specific support panels
- `assets/css/admin.css` — credential field styles, support panel variations
- `CHANGELOG.md` — updated with v3.4.1 changelog

### Package
- `/tmp/woosync-v3.4.1.zip` (92KB)

## $(date '+%H:%M') -- Additional XSS fixes for amrod-sync.php

Found 8 additional unescaped echoes during verification. Fixed:
- Line 547: `$status['sync_status']` → `esc_attr()` in class
- Line 549: `$last_sync` date → `esc_html()` in text
- Line 714: `$tier_active_since` date → `esc_html()` in text
- Line 720: `$tier_expiry` → `esc_html()` in text
- Line 1020: `$last_sync` date → `esc_html()` in heading
- Line 1409: `$tier_active_since` → `esc_attr()` in value
- Line 1416: `$tier_expiry` → `esc_attr()` in value
- Line 3255: `$tiered_enabled` ternary → `esc_html()` in text

Also fixed `$min_margin` at line 3282 (missed in initial batch).
